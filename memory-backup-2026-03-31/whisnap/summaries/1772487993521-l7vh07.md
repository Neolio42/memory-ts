---
id: "1772487993521-l7vh07"
created: 1772487993522
updated: 1772487993522
session_id: "bc2a589d-c2f0-4da1-9025-668aa11e0fdd"
project_id: "-Users-ned-Desktop-Projects-whisnap"
interaction_tone: "Casual technical collaboration with the user testing the app in real-time and sharing raw log output"
---

[Part 1/3] Implemented the full Parakeet (sherpa-rs) re-integration into the Whisnap transcription pipeline: created ParakeetTranscriber and DynamicTranscriber, wired the orchestrator, added models.json entries, fixed FP16 detection, and simplified execution to CPU-only with adaptive threading. Then found and fixed a context-dropping bug causing misleading logs after every transcription. Concluded with research showing FluidAudio CoreML achieves 110x RTF on Apple Silicon vs 3-5x for sherpa-rs — decision made to replace sherpa-rs entirely with FluidAudio Swift SDK. Migration plan written; implementation started (Cargo.toml and models.json partially updated) but session ended mid-flight.

[Part 2/3] Implemented FluidAudio CoreML integration to replace sherpa-rs Parakeet in the Whisnap Tauri v2 macOS app. Created Swift FFI package (FluidAudioTranscriber), Rust FFI bridge, backend module, and transcriber component. Fixed multiple bugs in the model download flow (wrong event formats, missing DownloadManager registration, no cancellation support). Corrected model sizes (481MB/463MB not 2.67GB) and fixed pre-warm to actually load FluidAudio models instead of skipping.

[Part 3/3] Extended debugging and improvement session on Whisnap's FluidAudio CoreML integration. Fixed a 3.5s startup delay caused by check_model_exists not handling SdkManaged models, completed two full audit cycles (19 → 8 → 0 known issues), discovered the FluidAudio SDK has a manual offline loading API (AsrModels.load(from:)), and created a plan to switch from SdkManaged to proper Manifest-based downloads with model deletion support.